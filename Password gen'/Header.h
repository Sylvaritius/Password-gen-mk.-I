#pragma once
int genchar(int passType, char* passArray, int passLength);


// <stdio.h>

//int main(){
	//int size;
	//scanf(“ % d”, &size);
	//int arr[size]; //Must be defined with some value or a variable.
	//for (int i = 0; i < size; i++) arr[i] = i;
	//for (int i = 0; i < size; i++) printf("%d ", arr[i]);
	//return 0;
//}

















